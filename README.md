# Mexico Repair Flow
A small Python project for managing parts inventory and work orders.

## Usage
1. Run `python -m pytest` to run the tests.
2. Use the `inventory` module to manage parts and work orders.
